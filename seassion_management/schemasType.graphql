### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Admin {
  id: Int!
  username: String!
}

type AuthType {
  admin: Admin!
  token: String!
}

type Booking {
  BookingID: Int!
  QRCode: String!
  Status: BookingStatusEnum!
  allocatedTV: String!
  createdBy: Admin
  creatorId: Int!
  numberOfSessions: Int!
  userName: String!
}

enum BookingStatusEnum {
  ACTIVE
  UNACTIVE
}

type Mutation {
  createAdmin(password: String!, username: String!): Admin!
  createBooking(allocatedTV: String!, numberOfSessions: Int!, userName: String!): Booking!
  deleteAdmin(id: Int!): String!
  deleteBooking(bookingId: Int!): Booking!
  login(password: String!, username: String!): AuthType!
  pauseSession(bookingId: Int!, sessionId: Int!): Session
  register(password: String!, username: String!): AuthType!
  startSession(bookingId: Int!): Session
  updateAdmin(id: Int!, username: String!): Admin!
  updateBooking(bookingId: Int!, name: String, numberOfSessions: Int, tvAllocated: String): Booking!
}

type Query {
  Admins: [Admin!]!
  allBookings: [Booking!]!
  allSession: [Session!]!
  bookingById(id: Int!): Booking
  findAdmin(id: Int!): Admin
  findSessionById(id: Int!): Session
}

enum SeassionStatusEnum {
  ENDED
  PAUSED
  START
}

type Session {
  Status: SeassionStatusEnum!
  booking: Booking!
  bookingId: Int!
  endTime: String!
  id: Int!
  startTime: String!
  timeRemaining: Int!
}
